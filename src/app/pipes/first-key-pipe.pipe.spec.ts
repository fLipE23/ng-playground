import { FirstKeyPipePipe } from './first-key-pipe.pipe';

describe('FirstKeyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstKeyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
